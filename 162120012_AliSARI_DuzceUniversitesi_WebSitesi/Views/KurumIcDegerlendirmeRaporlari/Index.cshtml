@model IEnumerable<_162120012_AliSARI_DuzceUniversitesi_WebSitesi.Models.KurumIcDegerlendirmeRaporlari>

@{
    ViewData["Title"] = "Kurum İç Değerlendirme Raporları";
}
<html>
<head>

</head>
<body>
    <div class="ustkisim  row">
        <div class="col-sm-6 ">
            <div class="baslik">Düzce Üniversitesi</div>
            <ol class="breadcrumb icerik">
                <li class="breadcrumb-item ">
                    <a href="/" class="icerikilk">Anasayfa</a>
                </li>
                <li class="breadcrumb-item active">
                    Kurum İç Değerlendirme Raporları
                </li>
            </ol>
        </div>

        <div class="col-sm-6" style="text-align:right">
            <div>&nbsp;</div>
            <i class="fa fa-search"></i>
            <input class="arama" type="text" value="" placeholder="Arama">
        </div>
    </div>
    <div class="container-fluid" id="bodykismi">
        <h2 id="baslik1">Kurum İç Değerlendirme Raporları</h2>
        <br /><br />
        @{
            @foreach (var item in Model)
            {
                if ((item.Aciklama.IndexOf("Yılı", 0) >= 1))
                {
                    <p>
                        <a href="@item.Link" target="_blank" id="butcelink">@item.Aciklama</a> &nbsp;@item.YılAyrintilari
                    </p>
                }
            }
        }

        <br />
        @{
            @foreach (var item in Model)
            {
                if ((item.Aciklama.IndexOf("Yılı", 0) >= 1))
                {

                }
                else
                {
                    <p>
                        <a href="@item.Link" target="_blank" id="butcelink">@item.Aciklama</a> &nbsp;@item.YılAyrintilari
                    </p>
                }
            }
        }

        <br /><br />


    </div>


</body>
</html>